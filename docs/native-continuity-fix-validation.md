# 原生上下文接续修复与验证

日期：2026-09-10。

状态：源码修复与本地验收完成；不代表已安装、发布，或已验证外部模型的自主行为。

## 修复范围

沿用原生 Claude Code、私有 tmux、会话本地 hooks 和现有 History/Notes CLI：

- 自动操作暂停后，仍接受并核对迟到的真实 `SessionStart`，更新会话绑定并登记历史；正常 hook 继续观测用量，明确显示暂停原因。
- 在 `PostToolBatch` 之后、下一次模型请求前检查预算。当前直接调用结算即可换窗，不必等待后台任务全部结束。
- 换窗通过原生后台消息通道投递，以真实新会话事件和接续消息入史确认完成，不靠延迟或输入框外观推断成功。
- 已提交但尚未执行的输入保留在原生历史中，接续携带准确定位，按顺序处理；不另建用户输入队列，不将交接记录当成新增授权。
- 保留后台任务句柄和最近记录定位，避免为恢复结果重跑任务。未提交草稿不由控制器读取或改写。
- 支持的超大 Bash、文本 Read 和已完成 Agent 结果保留完整脱敏存档，返回有界预览。多模态结果不按 base64 字节冒充文本 token；不能忠实替换的结果交由批次边界处理。
- History/Notes 在整个任务期间通过原有 CLI 使用，历史登记不依赖模型是否主动保存交接。
- 控制器恢复前核对对应会话的原生通道环境与凭证。缺少凭证时不启动进程、不改运行状态；凭证只在进程环境中传递，不写入状态或报告。

已移除新增的 History/Notes MCP 实现、专属测试、插件注册、工具发现指引和字节码缓存。旧的 tmux 输入框注入函数及专属测试也已移除。`native_control.py` 与 `result_budget.py` 属于原定修复，不是 CPA 替代方案；未来探索仍单独保留在 [Codex/Pi 评估文档](future-codex-context-integration.md)。

## 实际验证

验证环境：macOS，Python 3.12，原生 Claude Code 2.1.266。没有外部模型请求、训练、GPU 或付费远程资源。

### 本地回归

- 定向回归：107 项通过，1.499 秒。
- 修复恢复入口后，完整本地回归：181 项通过，6.408 秒。
- `git diff --check` 通过。

等价运行命令（临时目录留在测试目录中）：

```sh
TMPDIR="$PWD/tests" PYTHONDONTWRITEBYTECODE=1 PYTHONPATH=src \
python -B -m unittest discover -s tests
```

首次恢复入口回归暴露了测试桩遗漏整数 PID，出现 `MagicMock is not JSON serializable`；补齐测试桩后，真实回归按预期失败：缺少凭证时没有抛出错误，仍走到了控制器启动。修复后失败路径、有效环境下的启动路径及完整套件均通过。这两次失败均保留在验证历史中，不解释为瞬时故障重试。

### 原生宿主集成

`tests/native_runtime_probe.py` 启动独立原生 TUI、私有 tmux 和 localhost 脚本响应服务，不连接用户正在使用的终端。模型响应、工具调用次序和 usage 由固定脚本提供；实际工具执行、hooks、原生会话事件、输入框、历史文件、CLI 检索和结果存档使用真实实现。

最终探针通过，耗时 12.565 秒：

| 检查 | 实际结果 |
| --- | --- |
| 原生新会话确认 | 通过 |
| 后台任务结束前换窗 | 通过 |
| 后台 producer 执行次数 | 1 次，并完成 |
| 接续消息真实入史 | 通过 |
| 两阶段 History CLI 搜索与精确读回 | 2 次读回均与实际输入原文匹配 |
| 换窗期间提交的消息 | 原生历史中恰好 1 条，后续精确读回 |
| 未提交草稿 | 保留，未变成用户消息 |
| 超大文本结果 | 模型请求中观测到 1 份有界结果 |
| 完整结果存档 | 实际解析通过，内容及 SHA256 匹配 |

此前两次逐步覆盖的探针也通过：基本换窗与 CLI 查史 12.568 秒；加入并发消息、草稿和精确结果校验后 13.060 秒。每次运行都有独立 `receipt.json` 和原生历史，探针返回其位置；本文件不收录本机绝对路径。

## v0.2.0 发布候选验证

版本号更新与 README 简化后，完整本地回归再次通过：181 项，5.819 秒。

发布 wheel 在不安装新依赖的情况下离线构建，随后检查 CRC、模块清单、所有 Python 模块与源码的逐字节一致性，以及排除私有数据和废案模块。再将 wheel 安装到独立测试目录，核对 `0.2.0` 版本、实际导入路径和 `claude-context` 入口，并从该安装运行同一原生集成探针：全部检查通过，12.165 秒。验证环境为 macOS arm64、Python 3.12.13、Claude Code 2.1.266。

这次隔离安装没有覆盖日常使用的 Python 环境。发布文件为 `claude_context_continuity-0.2.0-py3-none-any.whl`，SHA256：

```text
eee6cc9be81b740050381d634cfadb9d1aa51c8deb426a6413feb066cc4f6f59
```

## v0.2.1 恢复修复与发布候选验证

补充日期：2026-09-13。

本次修复原生恢复后的用户指令链与会话身份绑定：恢复窗口尚无真实用户指令时，仅从会话 ID、原生历史路径和工作目录匹配的已登记来源复用早期指令，并重新核验原始记录分类与哈希。没有可复用的登记时，保留实际恢复的会话及用量，进入只观测的暂停状态；后续真实用户记录通过核验后可恢复。接续消息本身不提供授权。另修复 CLI 把 `phase: paused` 当作成功的退出码错误，暂停回执现在返回 `2`。

- 首次新增回归的 3 项失败复现了缺少本窗口用户指令后回滚到错误旧会话的缺陷；修复后，恢复与状态层定向回归 69 项通过，历史解析与安全边界回归 26 项通过。
- 更新版本号后，完整本地套件 **191 项通过，6.927 秒**。测试配置与临时文件均使用隔离目录。
- 真实原生历史中的用户指令定位、哈希和跨窗口引用只读核验通过；未改写原始历史或活动会话状态。
- wheel 离线构建；CRC、文件清单、版本元数据及全部 **14 个 Python 模块**与源码的逐字节一致性检查通过。实际安装后，再次核对版本、导入路径、模块内容和 CLI 入口，均通过。
- 从已安装包运行同一隔离原生探针，在 **macOS、Python 3.12、Claude Code 2.1.270** 上全部通过，耗时 **11.805 秒**：后台 producer 仅运行一次，完成前成功换窗；接续真实入史；两次精确查史、单份并发输入、未提交草稿和完整结果存档均核验通过。

原生探针使用 localhost 脚本提供模型响应和 usage，没有外部模型调用。它验证本次安装与既有原生换窗链路的连接；同管理器、跨管理器恢复及拒绝篡改等恢复场景由定向 fixture 覆盖，未将这些 fixture 宣称为真实恢复 E2E。

发布文件为 `claude_context_continuity-0.2.1-py3-none-any.whl`，SHA256：

```text
736fb409084c3bfa180c60246ae51186b4c7a4c2de37de26d956d6a356540bfd
```

## 控制器可靠性与空闲读写修复

补充日期：2026-09-17。源码修复、主会话复核和本地验收完成；本节不表示已覆盖日常安装或重启已有会话。

继续使用私有 tmux 和原生消息通道。本次改动包括：

- 状态锁争用使用独立异常类型，控制器保留独占锁并等待后续观测，不因一次状态锁超时无声退出。其他未处理异常安全停止自动操作，并保留只含固定错误码的诊断；状态无法落盘时明确返回 `state_persisted: false`。诊断文件仅作故障说明，不用于授权或决定换窗。
- 稳定空闲边界复用来源元数据和状态摘要，不跨轮缓存历史正文；来源、会话、inode、长度、mtime、ctime 或状态变化时重新解析。每轮的边界检查和用量投影共用已验证记录；尾行尚未写完时等待，完整畸形记录仍拒绝。
- 相同状态不再重复替换文件或 fsync。非交互式 tmux 命令有 5 秒上限，交互式 attach 没有该超时；投递结果未知时仍不重发。
- `tui-status` 增加实际进程、控制器独占锁、tmux 和控制 socket 的只读检查；attach 明确区分“终端连上”和“控制器恢复”。恢复仍要求准确的受管通道环境，并在改变暂停状态前检查原生会话和通道是否可用。

实际验证：

- 完整离线套件 **208 项通过，6.534 秒**。覆盖锁争用后继续观测、不可写状态、重复落盘、空闲来源变化、半写入尾行、错误来源身份、恢复拒绝和交互 attach 的无超时边界。
- 稳定空闲的 6 轮定向检查：**完整历史解析 1 次、状态替换 0 次、fsync 0 次**；旧版同一类探针为 18 次解析、6 次状态替换、12 次 fsync。该对比是隔离 fixture，不声称是已部署版本的实时资源测量。
- 真实 19,631,006 bytes／594 条记录的本地历史只读解析通过；新投影与既有 public 方法完全相同，真实指令定位经 consumer 精确读回，来源大小与时间戳未变。另只读检查当前活动会话的进程和通道，确认其原有换窗已经完成。
- 沿用 `tests/native_runtime_probe.py`，在 macOS arm64、Python 3.12、Claude Code 2.1.270 上完成隔离原生集成，**通过，12.112 秒**。后台 producer 只执行一次，结束前完成换窗；接续入史、两次精确查史、单份并发输入、草稿保留和完整结果存档均通过。探针自建的 tmux 与 pane 已退出并核验，保留测试回执与证据。
- 第一次 TUI 回归错误地把“等待尾行完成”的阶段断言为 `rotation_requested`，实际应保留 `running`；修正该断言后，首次全量回归又发现同一新增 fixture 的 progress 记录漏了必需 `uuid`。两次失败均已保留，修正 fixture 后全量通过；没有放宽真实解析规则或原样盲目重试。
- `git diff --check` 通过；未安装依赖、调用外部模型、使用 GPU 或付费资源。原生集成仍由 localhost 脚本提供响应，不等同于外部模型自主行为或真实 VS Code 闪退恢复测试。

本次修复不把控制器缺陷当作历史 VS Code 闪退的已证实原因。源码验收时，日常安装仍为原有 0.2.1，当前活动控制器未被热替换或重启；后续安装验证见下节。

## v0.2.2 本地安装与发布候选验证

补充日期：2026-09-18。用户随后授权更新本地安装及 GitHub，按兼容性修复发布补丁版本 0.2.2。

- 更新版本号后，完整离线套件 **208 项通过，7.098 秒**。
- 使用现有依赖离线构建 wheel；CRC、模块白名单、版本元数据、CLI 入口及全部 **14 个 Python 模块**与已复核源码的逐字节一致性检查通过。
- 从该 wheel 更新本地安装，未访问依赖索引、未改变依赖版本；新进程的实际版本、导入位置、CLI 入口和模块内容全部匹配 0.2.2。
- 从**已安装包**运行同一隔离原生探针，运行时、hook 与 CLI 子进程均使用安装目录，不从源码导入实现。在 macOS arm64、Python 3.12.13、Claude Code **2.1.274** 上全部通过，**11.278 秒**：后台 producer 只执行一次，完成前成功换窗；接续真实入史；两次精确查史、单份并发输入、未提交草稿和完整结果存档均核验通过。自建 tmux 与 pane 已退出并核验，保留测试证据。
- 主会话复核及公开差异／发布包检查完成。没有更改全局模型、权限或 MCP 配置，也没有强制重启已有用户会话；新进程加载新版，运行中的旧控制器不会自动升级。

原生探针仍只连接 localhost 脚本响应服务，没有外部模型请求；它验证实际安装与原生换窗链路，不声称已重现真实 VS Code 闪退或完成真实断线恢复 E2E。

发布 wheel：`claude_context_continuity-0.2.2-py3-none-any.whl`，SHA256：

```text
7e70ef234f210a141fa9c5334e2393dd56cef42339757e313c667043f96a6440
```

## v0.2.3 原生 PR 关联元数据修复与验证

补充日期：2026-09-18。本节记录 0.2.3 的原生 PR 关联元数据修复及实际验证。

真实原生历史包含 `pr-link` 记录，其字段为 `type`、`sessionId`、`prNumber`、`prUrl`、`prRepository` 和 `timestamp`，没有消息 `uuid`。这不是旧会话格式迁移问题：任何包含此类原生 PR 关联元数据的会话都可能触发严格解析器的未知类型错误。

运行时行为只修改一处：把已确认的 `pr-link` 纳入现有状态元数据集合。解析仍先校验来源会话身份，不把 PR 元数据当作用户指令；其他未知类型仍拒绝。未增加身份迁移、宽松未知类型处理或绕过恢复校验的路径。

- 定向历史／索引／CLI 回归 **46 项通过，1.477 秒**；首次回归复现了未知 `pr-link` 错误，另有一处新测试误用索引查询参数 `role`，改为既有 `source_kinds` 后通过，首次输出保留。
- 加入“含无 uuid 的 PR 元数据仍可恢复到准确目标会话”的回归后，完整离线套件 **212 项通过，6.813 秒**；发布前对最终 0.2.3 再次全量检查，**212 项通过，6.835 秒**。
- 真实含 PR 元数据的恢复目标历史解析通过，用户指令 locator 经精确哈希读回通过；安装后再次通过实际 CLI 检索并精确读回原始用户指令。
- 离线构建并更新本地候选，全部 14 个安装模块与源码逐字节一致，实际导入版本为 0.2.3，未变更依赖。已有 0.2.2 发布资产保持不变。
- 安装时，当前运行会话已因先前 resume 失败而保留启动身份；仅替换安装不会重新发出原生 `SessionStart`，也不会让已有控制器自动加载新模块。随后用户正常退出并用原生 resume 重新打开，真实观测确认：安装与本会话插件均为 0.2.3，控制器绑定与实际原生会话一致，`phase=running`、`health=healthy`，暂停原因和诊断均为空，原生恢复确认次数为 1。未伪造 hook 事件、手工改状态或强行接管。此项证明真实会话恢复和当前监测健康，未额外触发强制换窗。

## PostToolBatch 延迟落盘导致自动换窗停滞

补充日期：2026-09-18。用户在实际安装的 0.2.3 中复现：批次 hook 显示停止旧回合后没有自动换窗，直到再次提交输入才继续。此前的恢复健康检查和既有探针没有覆盖这一时序，不能据此认定该缺陷已修复。

原因是 hook 可能先于当前响应写入原生历史。原实现把当时可见的上一轮 `usage_locator` 保存为批次边界，随后要求最新记录与它完全相等；当前响应落盘后，这个条件不再成立。新用户输入会清除该边界并走延后输入路径，因此会暂时解除停滞。

修复仍使用既有批次 hook、控制器及原生消息通道：记录本批工具调用 ID，等待相应调用和全部结果实际入史，并确认最新模型响应属于本批。允许同一响应的后续记录落盘，保留会话与回合身份、原用量记录 hash、未结算工具及用户指令校验；不发送假 prompt、不修改原生历史、不重放任务。

当前实际验证：

- 新增的延迟落盘、同响应多记录、全部调用／结果就绪及原快照变动检查，首次运行共 6 项、4 项失败，复现旧实现的永久等待及记录未落盘时可能提前清空的问题；首次失败保留。
- 修复后 TUI 定向回归 **84 项通过，1.755 秒**；完整离线套件 **217 项通过，7.274 秒**。
- 对真实停滞会话历史进行只读 predicate 重放：旧用量快照为 381,993 tokens，后来落盘的当前响应为 383,328 tokens；修复后无需追加用户输入即可确认批次就绪。原历史 SHA256 未变，未向活动会话投递任何命令。
- 将接续目标 fixture 收紧为空历史、不预写新用户消息后，受影响的 6 项批次回归再次通过，0.134 秒。
- 新增 `tests/native_runtime_probe.py --batch-only`：仅提交一次初始输入，实际执行一次同步 `Read`，不启动后台 producer、不补发用户输入、不注入草稿；以真实 hook 停止记录、两代原生历史和模型响应 ID 验证自动换窗及接续。首次通过耗时 **1.401 秒**；主会话复核后修正探针对“同一次响应迟到记录”和“新模型请求”的区分，最终再次通过，**1.373 秒**。实际只存在 1 条初始用户消息，旧窗口停止后没有新模型请求，完成响应绑定在新窗口。
- 原有原生探针仍通过，**12.603 秒**：后台 producer 仅运行一次，结束前完成换窗；两次准确查史、并发输入仅一份、草稿保留、结果限长及完整存档核验均通过。
- 三次原生探针均运行于 macOS、Python 3.12、Claude Code **2.1.274**；真实宿主与工具配合 localhost 脚本模型及 usage，无外部模型请求。各自创建的 tmux server 和原生 pane 进程均已退出并核验；保留日志、回执及历史证据。
- 主会话完成源码、测试和原生验收的范围复核，`git diff --check` 通过。本批源码验收时未覆盖日常安装、重启已有控制器、提交或发布；后续本地更新见下一节。原控制器不会因为源码改变而自动加载修复。

可从仓库根目录显式运行新探针（默认模式仍为原有完整链路）：

```sh
PYTHONDONTWRITEBYTECODE=1 PYTHONPATH=src python -B tests/native_runtime_probe.py --batch-only
```

## 0.2.4 本地安装验证

补充日期：2026-09-18。用户随后授权更新本地安装；本次将已验收修复提升为补丁版本 0.2.4，未提交或发布到远程。

- 安装前核对上一节复核回执的源码 hash，全部一致；原安装的 14 个模块也与 0.2.3 安装回执一致。只更新版本元数据，不改变已验收的运行时修复。
- 使用现有构建依赖离线生成 wheel。CRC、模块白名单、版本、两个 CLI 入口及全部 14 个模块与源码的逐字节校验通过；随后离线安装，其他依赖版本集合未变。
- 新进程的实际导入版本为 **0.2.4**，全部安装模块 hash 与已核验 wheel 一致，原 `cclaude` 入口保持不变。
- 从**已安装包**运行无追加输入的原生探针：仅把现有探针的父进程 import 路径及 hook／CLI 子进程 `PYTHONPATH` 指向安装位置，未改场景、响应或验收条件。实际加载的运行时模块和新建会话插件均核对为安装版；在 Claude Code **2.1.274** 上通过，耗时 **1.378 秒**。一次初始输入、一次同步 `Read` 后自动换窗，接续和完成响应均绑定新会话，旧会话停止后无额外模型请求。
- 探针仍只使用 localhost 脚本模型和 usage，没有外部模型请求；其 tmux server 与原生 pane 进程已退出并核验。已有用户控制器未被重启、接管或热替换；要让已运行的旧控制器加载新版，需正常退出后重新使用 `cclaude` 恢复。

本地安装 wheel：`claude_context_continuity-0.2.4-py3-none-any.whl`，SHA256：

```text
7cfc944ebad8e4fd703e876d5563c658a9bf3e68003f967b6618650d2955cb97
```

## 复核与边界

主会话已复核会话绑定、暂停恢复、输入和工具结算、存档连接、MCP 清理以及中英文说明的适用范围。

- 上述集成证明原生宿主与 CLI/存档的真实连接，不证明外部模型会自主选择 History 工具，也不等同于真实大上下文模型压力测试。
- 原生通道及批次 hook 分别在上述 2.1.266 和 2.1.270 版本实际核验。其他版本仍需实际兼容性检查，不能只凭版本号声称全部行为已验证。
- 运行中的已有会话不会因为源码修改而被主动重启、接管或迁移；安装和发布状态须单独报告。
